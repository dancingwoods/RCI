# grouped data Random Forest algorithm.

#-
#' Creates and trains a custom random forest classifier
#' 
#' @param labels a vector giving the labels of the training data.  this should
#' be a factor
#' @param groups a vector giving the grouping of the training data.  Any data points
#' the same group value are treated as members of the same group.
#' @param data a data.frame giving the training data features
#-
GrpRFCreate <- function(labels, groups, data){
	
}

GrpTreeCreate <- function(labels, groups, data){
	
	
}

ChooseSplit <- function(labels, groups, )